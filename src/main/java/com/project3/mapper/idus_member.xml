<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapper.member">
	<!-- insert:join -->
	<insert id="join" parameterType="com.project3.vo.IdusMemberVO">
		insert into idus_member values(
		#{uemail}, #{upass}, #{uname}, #{cp}, '', '', '', #{aggrsms}, #{aggremail}, '', ''
		)
	</insert>
	
	<!-- select:login -->
	<select id="login" parameterType="com.project3.vo.IdusMemberVO" resultType="com.project3.vo.IdusSessionVO">
		select count(*) result, uname from idus_member 
				WHERE uemail=#{uemail} AND upass=#{upass} group by uname
	</select>
	
	<!-- select:list -->
	<select id="list" resultType="com.project3.vo.IdusMemberVO">
		select rownum rno, uemail, upass, uname, cp, addr1, addr2, addr3, aggrsms, aggremail, pfile, psfile from idus_member
	</select>
</mapper>